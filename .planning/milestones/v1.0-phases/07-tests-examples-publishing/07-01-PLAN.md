---
phase: 07-tests-examples-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/__tests__/sessions.test.ts
  - packages/server/src/__tests__/document.test.ts
  - packages/server/src/__tests__/face.test.ts
  - packages/server/src/__tests__/identity.test.ts
  - packages/server/src/deepidv.test.ts
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Every public SDK method has at least one happy-path and one error-path test"
    - "pnpm test passes with all existing + new tests green"
    - "Tests follow established pattern: real HttpClient + msw interception, server.use() per-test"
  artifacts:
    - path: "packages/server/src/__tests__/sessions.test.ts"
      provides: "Error-path tests for retrieve(404), list(500), updateStatus(404), create(401)"
    - path: "packages/server/src/__tests__/document.test.ts"
      provides: "Error-path tests for scan(401, 500)"
    - path: "packages/server/src/__tests__/face.test.ts"
      provides: "Error-path tests for detect(401/500), compare(missing source), estimateAge(401/500)"
    - path: "packages/server/src/__tests__/identity.test.ts"
      provides: "Error-path tests for verify(401/500)"
    - path: "packages/server/src/deepidv.test.ts"
      provides: "Tests for maxRetries:0 acceptance and uploadTimeout config"
  key_links:
    - from: "All server test files"
      to: "msw handlers via server.use()"
      via: "per-test handler override returning 401/404/500"
      pattern: "server\\.use\\(.*http\\."
---

<objective>
Fill test gaps across all server module tests per D-01: every public method gets at least one happy-path AND one error-path test. Add ~12-15 new test cases to existing test files.

Purpose: Satisfy TEST-01 (unit test completeness) and TEST-02 (integration test coverage for each module's request/response mapping). No new test files — gaps are in existing files.
Output: All 5 server test files updated with error-path tests; `pnpm test` passes with increased test count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tests-examples-publishing/07-CONTEXT.md
@.planning/phases/07-tests-examples-publishing/07-RESEARCH.md

@packages/server/src/__tests__/sessions.test.ts
@packages/server/src/__tests__/document.test.ts
@packages/server/src/__tests__/face.test.ts
@packages/server/src/__tests__/identity.test.ts
@packages/server/src/deepidv.test.ts
@packages/server/src/__tests__/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error-path tests to sessions, document, face, and identity test files</name>
  <files>
    packages/server/src/__tests__/sessions.test.ts
    packages/server/src/__tests__/document.test.ts
    packages/server/src/__tests__/face.test.ts
    packages/server/src/__tests__/identity.test.ts
  </files>
  <read_first>
    packages/server/src/__tests__/sessions.test.ts
    packages/server/src/__tests__/document.test.ts
    packages/server/src/__tests__/face.test.ts
    packages/server/src/__tests__/identity.test.ts
    packages/server/src/__tests__/setup.ts
    packages/core/src/__tests__/client.test.ts
  </read_first>
  <action>
Add error-path tests to each file using the established pattern (per D-03): real HttpClient + msw interception, `server.use()` per-test with `http.post`/`http.get`/`http.patch` returning error responses.

**sessions.test.ts — add 4 tests:**
1. `Sessions.retrieve returns AuthenticationError on 401` — `server.use(http.get('*/v1/sessions/:id', () => HttpResponse.json({ error: 'Unauthorized' }, { status: 401 })))` — assert throws `AuthenticationError`
2. `Sessions.list returns DeepIDVError on 500` — `server.use(http.get('*/v1/sessions', () => HttpResponse.json({ error: 'Internal Server Error' }, { status: 500 })))` — assert throws `DeepIDVError` with status 500
3. `Sessions.updateStatus returns DeepIDVError on 404` — `server.use(http.patch('*/v1/sessions/:id', () => HttpResponse.json({ error: 'Not Found' }, { status: 404 })))` — assert throws `DeepIDVError` with status 404
4. `Sessions.create returns AuthenticationError on 401` — override the presign + create handlers to return 401 on the create call — assert throws `AuthenticationError`

**document.test.ts — add 2 tests:**
1. `Document.scan returns AuthenticationError on 401` — override presign handler to return 401 — assert throws `AuthenticationError`
2. `Document.scan returns DeepIDVError on 500` — override the POST /v1/document/scan handler (after presign succeeds) to return 500 — assert throws `DeepIDVError` with status 500

**face.test.ts — add 3 tests:**
1. `Face.detect returns AuthenticationError on 401` — override presign to return 401 — assert throws `AuthenticationError`
2. `Face.compare throws ValidationError when source image is missing` — call `face.compare({ image1: undefined, image2: validBuffer })` (or equivalent invalid input per the schema) — assert throws `ValidationError`
3. `Face.estimateAge returns DeepIDVError on 500` — override presign or estimateAge endpoint to return 500 — assert throws `DeepIDVError`

**identity.test.ts — add 2 tests:**
1. `Identity.verify returns AuthenticationError on 401` — override presign to return 401 — assert throws `AuthenticationError`
2. `Identity.verify returns DeepIDVError on 500` — override the POST /v1/identity/verify handler to return 500 — assert throws `DeepIDVError`

**Pattern for error tests (copy from existing client.test.ts error mapping tests):**
```typescript
it('methodName returns AuthenticationError on 401', async () => {
  server.use(
    http.post('*/v1/endpoint', () =>
      HttpResponse.json({ error: 'Unauthorized' }, { status: 401 }),
    ),
  );
  await expect(module.method(validInput)).rejects.toThrow(AuthenticationError);
});
```

Import `AuthenticationError` and `DeepIDVError` from `@deepidv/core` in any file that does not already import them.
  </action>
  <verify>
    <automated>cd C:/Users/omart/subprime/deepidv-sdk-ts && pnpm --filter @deepidv/server test</automated>
  </verify>
  <acceptance_criteria>
    - sessions.test.ts contains `returns AuthenticationError on 401` test for retrieve
    - sessions.test.ts contains `returns DeepIDVError on 500` test for list
    - sessions.test.ts contains `returns DeepIDVError on 404` test for updateStatus
    - sessions.test.ts contains `returns AuthenticationError on 401` test for create
    - document.test.ts contains `returns AuthenticationError on 401`
    - document.test.ts contains `returns DeepIDVError on 500`
    - face.test.ts contains `returns AuthenticationError on 401` for detect
    - face.test.ts contains `ValidationError when source image is missing` or equivalent
    - face.test.ts contains `returns DeepIDVError on 500` for estimateAge
    - identity.test.ts contains `returns AuthenticationError on 401`
    - identity.test.ts contains `returns DeepIDVError on 500`
    - `pnpm --filter @deepidv/server test` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 4 server module test files have error-path tests for every public method. At least 11 new test cases added. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add deepidv.test.ts config edge-case tests and remove passWithNoTests flag</name>
  <files>
    packages/server/src/deepidv.test.ts
    packages/server/vitest.config.ts
  </files>
  <read_first>
    packages/server/src/deepidv.test.ts
    packages/server/vitest.config.ts
    packages/server/src/deepidv.ts
  </read_first>
  <action>
**deepidv.test.ts — add 2 tests:**
1. `DeepIDV accepts maxRetries: 0 without throwing` — `const client = new DeepIDV({ apiKey: 'sk-test', maxRetries: 0 }); expect(client.sessions).toBeDefined();` — confirms zero retries is valid config
2. `DeepIDV accepts uploadTimeout config without throwing` — `const client = new DeepIDV({ apiKey: 'sk-test', uploadTimeout: 60000 }); expect(client.sessions).toBeDefined();` — confirms uploadTimeout is accepted

**vitest.config.ts — remove passWithNoTests:**
Remove `passWithNoTests: true` from the test config object. The server package now has 46+ tests, making this flag unnecessary. The resulting config should be:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    setupFiles: ['./src/__tests__/setup.ts'],
  },
});
```
  </action>
  <verify>
    <automated>cd C:/Users/omart/subprime/deepidv-sdk-ts && pnpm --filter @deepidv/server test</automated>
  </verify>
  <acceptance_criteria>
    - deepidv.test.ts contains `maxRetries: 0` in a test case
    - deepidv.test.ts contains `uploadTimeout` in a test case
    - packages/server/vitest.config.ts does NOT contain `passWithNoTests`
    - `pnpm --filter @deepidv/server test` exits 0
  </acceptance_criteria>
  <done>DeepIDV constructor has config edge-case tests. passWithNoTests flag removed. All server tests pass.</done>
</task>

</tasks>

<verification>
Run `pnpm test` (full suite across core + server). All 172+ tests pass (existing 172 + ~13 new = ~185).
</verification>

<success_criteria>
- Every public method in sessions, document, face, identity, and DeepIDV has at least one happy-path and one error-path test (per D-01)
- Tests follow D-03 patterns: real HttpClient + msw, server.use() per-test, onUnhandledRequest: error
- passWithNoTests removed from server vitest config
- `pnpm test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-tests-examples-publishing/07-01-SUMMARY.md`
</output>
